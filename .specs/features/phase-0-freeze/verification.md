# Phase 0 — Freeze the target verification

**Verdict**: PASS
**Profile**: light
**Diff range**: 9dcf2bcd..HEAD
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

Fault injection was not run because profile is light, so a missing Faults injected killed-mutant table is not a forgotten step. Coverage was not recomputed from authority (light). Binding sources were not opened as a required section (standard/ui only).

Proofs re-run at HEAD `a603c91f` by the verifier (not the author's claim):

`node --test scripts/architecture-guidance.test.mjs scripts/architecture-cruiser.test.mjs`

20 tests, 20 pass, 0 fail. Each named pattern in checks.md exists (`rg` on the two spec files) and appears as `✔` in that output. Both spec files are in `9dcf2bcd..HEAD`.

## Checks

| Check | Claim                                                                                                                                                                                 | Proof run                                                                                   | Evidence                                                                                                                                                                                                                                                                                                                          | Result |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| C1    | CLAUDE.md overview Arch pointer names `docs/architecture-refactoring-roadmap.md`                                                                                                      | batch; `✔ overview Arch pointer names the roadmap`                                          | `scripts/architecture-guidance.test.mjs:29` - `assert.equal(match[1], ROADMAP)` with `ROADMAP = 'docs/architecture-refactoring-roadmap.md'`                                                                                                                                                                                       | PASS   |
| C2    | CLAUDE.md doc-index table contains a row for `docs/architecture-refactoring-roadmap.md`                                                                                               | batch; `✔ doc-index row names the roadmap`                                                  | `scripts/architecture-guidance.test.mjs:34` - `assert.match(index, /<bar> \`docs\/architecture-refactoring-roadmap\.md\` <bar>/)`where`<bar>` is the markdown table delimiter in the test                                                                                                                                         | PASS   |
| C3    | CLAUDE.md `bens-ddd-module` bullet names ADR-2 explicit composition and does not tell the reader to add abstract-class DI tokens                                                      | batch; `✔ DDD skill bullet names ADR-2 composition and forbids abstract-class tokens`       | `scripts/architecture-guidance.test.mjs:42-45` - `assert.match(bullet, /ADR-2/)`; `assert.match(bullet, /composi[cç][aã]o/i)`; `assert.doesNotMatch(bullet, /abstract class tokens/i)`; `assert.doesNotMatch(bullet, /classes abstratas/i)`                                                                                       | PASS   |
| C4    | MOD-1 summary-table cell mentions ADR-2 and explicit composition and does not prescribe abstract-class DI tokens                                                                      | batch; `✔ MOD-1 summary row names ADR-2 composition and does not prescribe abstract tokens` | `scripts/architecture-guidance.test.mjs:53-56` - `assert.match(row, /ADR-2/)`; `assert.match(row, /composi[cç][aã]o/i)`; `assert.doesNotMatch(row, /classes abstratas/i)`; `assert.doesNotMatch(row, /abstract class tokens/i)`                                                                                                   | PASS   |
| C5    | MOD-1 Decisão replaces the abstract-class-tokens bullet with explicit composition and ADR-2 and links the roadmap                                                                     | batch; `✔ MOD-1 Decisao bullet names explicit composition ADR-2 and links the roadmap`      | `scripts/architecture-guidance.test.mjs:66-69` - `assert.match(decisao, /ADR-2/)`; `assert.match(decisao, /composi[cç][aã]o/i)`; `assert.match(decisao, /architecture-refactoring-roadmap\.md/)`; `assert.doesNotMatch(decisao, /Tokens de DI são classes abstratas/)`                                                            | PASS   |
| C6    | MOD-1 Migração names the roadmap as the sequence and does not present the old seven-phase 2026-09-13 path as the path to execute                                                      | batch; `✔ MOD-1 Migracao sequence names the roadmap and not the seven-phase path`           | `scripts/architecture-guidance.test.mjs:79-80` - `assert.match(migracao, /architecture-refactoring-roadmap\.md/)`; `assert.doesNotMatch(migracao, /7 fases incrementais descritas em/)`                                                                                                                                           | PASS   |
| C7    | every line in CLAUDE.md or ARCHITECTURE-DECISIONS.md that contains `abstract class tokens` or `classes abstratas` also contains `superseded`                                          | batch; `✔ live guidance phrases only appear with superseded`                                | `scripts/architecture-guidance.test.mjs:89-93` - `assert.match(line, /superseded/i, \`${file} line mentions abstract tokens without superseded: ${line}\`)`over files`CLAUDE.md`and`docs/ARCHITECTURE-DECISIONS.md`. At HEAD the `if (needle.test(line))` body has zero hits (phrases absent); the IF is false so the THEN holds. | PASS   |
| C8    | migration-plan starts with a banner stating superseded for sequencing and DI, remains a move catalogue, and points at the roadmap                                                     | batch; `✔ migration-plan banner states superseded sequencing DI catalogue and roadmap`      | `scripts/architecture-guidance.test.mjs:105-109` - `assert.match(banner, /superseded/i)`; `assert.match(banner, /sequencing/i)`; `assert.match(banner, /\bDI\b/)`; `assert.match(banner, /catalogue or catalog/i)` (test uses regex alternation); `assert.match(banner, /architecture-refactoring-roadmap\.md/)`                  | PASS   |
| C9    | migration-plan from the first `# Migration Plan` heading to EOF is byte-identical to the pre-change snapshot                                                                          | batch; `✔ migration-plan body unchanged from the first Migration Plan heading`              | `scripts/architecture-guidance.test.mjs:118` - `assert.equal(body, snapshot)` after `body = text.slice(headingAt)`                                                                                                                                                                                                                | PASS   |
| C10   | `docs/architecture/2026-09-13-modular-architecture.md` sha256 matches the pre-change snapshot                                                                                         | batch; `✔ modular-architecture unchanged`                                                   | `scripts/architecture-guidance.test.mjs:126` - `assert.equal(digest, MODULAR_ARCHITECTURE_SHA256)` (`12ae08d5c505356708c3840bb41023c3ae2307882b72b7c86a473a6816d65853`)                                                                                                                                                           | PASS   |
| C11   | `context-map.md` status paragraph names the roadmap; module table and may-import table match the pre-change snapshots                                                                 | batch; `✔ context-map status and tables`                                                    | `scripts/architecture-guidance.test.mjs:140` - `assert.match(status, /architecture-refactoring-roadmap\.md/)`; `:143-148` and `:149-154` - `assert.equal(tableLines(modules), tableLines(snapshot))` and `assert.equal(tableLines(imports), tableLines(snapshot))`                                                                | PASS   |
| C12   | root `package.json` lists `dependency-cruiser` in `devDependencies` and script `arch:check` includes `--config .dependency-cruiser.cjs`                                               | batch; `✔ package.json arch:check lists dependency-cruiser and the config flag`             | `scripts/architecture-cruiser.test.mjs:50-56` - `assert.ok(pkg.devDependencies && pkg.devDependencies['dependency-cruiser'], ...)`; `assert.match(script, /--config \.dependency-cruiser\.cjs/)`                                                                                                                                  | PASS   |
| C13   | `.dependency-cruiser.cjs` exports forbidden rule `routes-no-db` with `severity` `warn`, `from.path` matching `apps/server/src/routes`, `to.path` matching `@repo/db` or `packages/db` | batch; `✔ rule routes-no-db is warn from server routes to db`                               | `scripts/architecture-cruiser.test.mjs:61-63` - `assert.equal(rule.severity, 'warn')`; `assert.match(String(rule.from.path), /apps\/server\/src\/routes/)`; `assert.match(String(rule.to.path), /@repo\/db or packages\/db/)` (test uses regex alternation)                                                                       | PASS   |
| C14   | `.dependency-cruiser.cjs` exports forbidden rule `no-circular` with `severity` `warn` and `to.circular` true covering `packages/core/src/modules`                                     | batch; `✔ rule no-circular is warn over core modules`                                       | `scripts/architecture-cruiser.test.mjs:68-70` - `assert.equal(rule.severity, 'warn')`; `assert.equal(rule.to.circular, true)`; `assert.match(String(rule.from.path), /packages\/core\/src\/modules/)`                                                                                                                             | PASS   |
| C15   | `pnpm arch:check` against the current tree prints a line matching `violations` and exits 0                                                                                            | batch; `✔ arch:check prints violations and exits 0`                                         | `scripts/architecture-cruiser.test.mjs:80-81` - `assert.equal(result.status, 0, output)`; `assert.match(output, /violations/i)`                                                                                                                                                                                                   | PASS   |
| C16   | depcruise with this config on an isolated tree whose `apps/server/src/routes/canary-forbidden-db-import.ts` imports `@repo/db` reports rule `routes-no-db`                            | batch; `✔ canary reports routes-no-db`                                                      | `scripts/architecture-cruiser.test.mjs:89-91` writes `import { prisma } from '@repo/db'` into that canary path; `:105` - `assert.match(output, /routes-no-db/, output)`                                                                                                                                                           | PASS   |
| C17   | `docs/architecture/forbidden-deps.md` lists `routes/** → @repo/db` and the four core cycles                                                                                           | batch; `✔ forbidden-deps hotspots list routes to db and four core cycles`                   | `scripts/architecture-cruiser.test.mjs:113` - `assert.match(text, /routes\/\*\* → @repo\/db/)`; `:114-120` - `assert.match(text, new RegExp(cycle))` for `proposal⇄contact`, `proposal⇄policy`, `proposal⇄document`, `goal⇄dashboard`                                                                                             | PASS   |
| C18   | `.github/workflows/ci.yml` has a step whose `run` is `pnpm arch:check` and `continue-on-error` is `true`                                                                              | batch; `✔ CI arch:check continue-on-error`                                                  | `scripts/architecture-cruiser.test.mjs:127-128` - `assert.ok(arch, 'missing CI step run: pnpm arch:check')`; `assert.equal(arch.continueOnError, true)`                                                                                                                                                                           | PASS   |
| C19   | the `validate` job still lists `pnpm lint`, `pnpm typecheck`, and `pnpm test` after the `arch:check` step                                                                             | batch; `✔ CI blocking steps after arch:check`                                               | `scripts/architecture-cruiser.test.mjs:137-141` - `assert.notEqual(index, -1, \`missing ${command}\`)`; `assert.ok(index > archIndex, \`${command} must run after arch:check so continue-on-error still reaches it\`)`                                                                                                            | PASS   |
| C20   | the `validate` job steps whose `run` is `pnpm lint`, `pnpm typecheck`, or `pnpm test` do not set `continue-on-error`                                                                  | batch; `✔ CI blocking steps stay blocking`                                                  | `scripts/architecture-cruiser.test.mjs:150` - `assert.equal(step.continueOnError, false, \`${command} must not continue-on-error\`)` for those three commands                                                                                                                                                                     | PASS   |

## Coverage

n/a - profile light. The Coverage join was not recomputed from authority.

## Faults injected

n/a - profile light. Fault injection was not run because profile is light, so a missing Faults injected section is not a forgotten step.

## Swept existing

Re-read against the tree at HEAD. No `existing` row cites a missing constraint.

| Dimension          | Check | Constraint in code                                                                                                                                        | Present |
| ------------------ | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| validation         | C13   | `.dependency-cruiser.cjs:5-10` - `name: 'routes-no-db'`, `severity: 'warn'`, `from.path: '^apps/server/src/routes'`, `to.path: '@repo/db or packages/db'` | yes     |
| validation         | C14   | `.dependency-cruiser.cjs:13-18` - `name: 'no-circular'`, `severity: 'warn'`, `from.path: '^packages/core/src/modules'`, `to.circular: true`               | yes     |
| failure modes      | C19   | `.github/workflows/ci.yml:31-39` - `run: pnpm arch:check` then later `run: pnpm lint`, `pnpm typecheck`, `pnpm test`                                      | yes     |
| dependency failure | C19   | same YAML order as C19; `continue-on-error: true` on the arch step is C18 (`ci.yml:32-33`)                                                                | yes     |
| observability      | C15   | named test spawned `pnpm arch:check` at HEAD; `assert.equal(result.status, 0)` and `assert.match(output, /violations/i)` passed                           | yes     |

n/a rows (idempotency, authorization, concurrency, data lifecycle, state transitions) are policy the user approved; nothing in the code for them to be wrong about.

## Gate

`node --test scripts/architecture-guidance.test.mjs scripts/architecture-cruiser.test.mjs` - 20 passed, 0 failed
