# After-action: SCRUM-58

**PR:** #318 (MERGED)  
**Slug:** scrum-58-retirar-informacao-empresarial  
**Duration:** ~23.5 hours (paused 13 minutes mid-session, resumed 17h 11m later)

## Métricas

- Phases completed: 10/14 (READ_TICKET → CI_WATCH; Phases 11-14 pós-merge)
- Failures: 1 (CODE_REVIEW: 3 CRITICAL as casts → resolved via inline fallback)
- User checkpoints: 5 (BRAINSTORM_SPEC ambiguity approval, WRITE_PLAN approval/stop, IMPLEMENT ARCH_DECISION, RESUME trigger)
- User interventions extras: 0
- Commits: 6 (1 enum/helper + 3 feature + 1 Orval regen + 1 refactor)
- Files changed: 18 (10 auto-generated by Orval, 8 hand-written)

## O que deu certo

1. **Spec/Plan workflow funcionou sem retrabalho** — ambiguidade escalada (branchType vs businessActivity) foi resolvida via user checkpoint; plano foi sequencial sem pivots pós-implementação.

2. **TDD red/green disciplina efetiva** — 2/2 test-first tasks (`branch-field-sets-property/business.spec.tsx` + `insured-object-details-schema.spec.ts`) passfirst em green phase.

3. **Code review loop ágil** — 3 CRITICAL flagged (as casts), refactor feito inline (~36 linhas alteradas em business.tsx), re-review confirmou completo em 1 tentativa.

4. **Type guard pattern aplicado corretamente** — `isBusinessSegment()` criada em `packages/shared` exportada e utilizada em 6+ pontos do business.tsx sem reincidências de type assertions.

5. **Orval workflow funcionou fim-a-cabo** — regen produziu 10 files (8x Details models + 2x schema Zod) sem conflitos; enums bateram com shared package.

6. **QA Playwright via MCP funcionou** — 7/7 cenários core OK; 1 CRITICAL pré-existente (CNPJ InputMask) foi escalado corretamente como out-of-scope.

7. **Multi-session state preservation** — pausa às 03:50 + resume às 21:01 (mesma data) manteve contexto: state file preservado, spec/plan lidos corretamente, commits listados acertadamente.

## O que precisou retrabalho

1. **Orval regen workflow exigiu decisão arquitetural do user** — pra regenerar client, precisei:
   - Parar server :3001 do main checkout
   - Subir server em worktree :3001
   - Correr `pnpm --filter @app/web generate:api` (conecta em :3001)
   - Parar worktree, voltar ao main

   User respondeu AskUserQuestion pra validar essa orquestração. Sem helper automático, recai em manual decision.

2. **BusinessDetails tipo duplicado em constants.ts** — Orval gera `AdvanceProposal200DataDetails`, etc.; constants.ts também define `interface BusinessDetails { ... }`. Mantive ambas (Orval = API contract, constants = domain model), mas causou fricção inicial (qual atualizar?).

3. **Code reviewer falso positivo (memoryado)** — flagged "Select + items prop duplica visual" mas pattern existe em produção (residential.tsx, auto-fields.tsx). Detectado via memory `feedback_bens-code-reviewer-may-hallucinate`, skipado corretamente.

## Padrões observados

1. **Enum + Label mapping pattern replicado** — essa é a 3ª feature com `SOMETHING_VALUES[]`, `SOMETHING_LABELS`, `SOMETHING_OPTIONS`:
   - SCRUM-58: `BUSINESS_SEGMENT_VALUES/LABELS/OPTIONS`
   - SCRUM-70: `VEHICLE_TYPES_VALUES/LABELS`
   - SCRUM-68: proposal stage badges

   → Oportunidade de skill/helper consolidado.

2. **Type guard export frequency** — 4+ commits recentes fizeram type guard refactors (`isBusinessSegment`, `isProposalDetail`, etc.). Padrão está consolidado; CLAUDE.md não precisa update, mas bens-code-rules skill deveria reforçar pra novos features.

3. **Phase 5 → Phase 8 não autônomo** — fato: orchastrator travou em Phase 5 porque PR-3/4/5 não tinham mergeado. User teve que retomar manualmente. Pattern: novos harness features não ficam ready até PR-N merge real.

4. **Worktree :3000 CORS conflict conhecido** — memory `server-cors-pinned-to-3000` menciona :3000 pinning, mas Orval gen usa :3001. Não colidiu aqui, mas é land mine pra QA com login em worktree.

## Propostas

### Memory (auto-commit pelo orchestrator)

1. **`feedback_orval-regen-workflow-decision.md`**
   - **Why:** Orval regen exige parar/iniciar server, decisão arquitetural recorrente pra agendar PRs com feature novas (POST /details com novo campo = Orval regen)
   - **How to apply:** Quando PR adiciona campo a Details schema:
     1. Spec/plan já menciona "Orval regen obrigatório"
     2. IMPLEMENT phase: após feature funciona, preparar commit chore(web): regen Orval
     3. Decidir: _now_ (pause main :3001, subir worktree) vs _later_ (deixar pra code review phase)
     4. Default: _now_ (mais limpo, evita autogen no mesmo commit de refactor)
   - **Success signal:** Orval regen feita antes de refactor code review, causando 1 commit chore(web) separado

2. **`feedback_type-duplicates-orval-constants.md`**
   - **Why:** Padrão de domain model interface em constants.ts + Orval-generated types causa duplicação deliberada mas confusa. User pode atualizar errado.
   - **How to apply:** Documentar na spec que ambos existem:
     - `@/api/model/*Details` (Orval from OpenAPI) = API contract, read-only
     - `features/proposals/lib/constants.ts BusinessDetails` = domain UI model, hand-write
     - Quando update Details schema: Orval auto-regenera models, manual update constants (se BusinessDetails divergiu)
   - **Success signal:** PR implementando Details change menciona "updated both Orval model + constants UI type" no plan

### Repo (PR separada via orchestrator)

1. **Editar `.claude/agents/bens-implementation-flow.md` — seção "Orval regen workflow"**
   - **Reason:** Documentar que IMPLEMENT phase de feature com new Details field inclui Orval regen step; when to regen (now vs post-refactor).
   - **Suggested diff:**

   ```markdown
   #### Orval Client Regeneration (when Details schema changes)

   If the plan includes new fields in POST /details body or response (insured object details):

   1. After feature is functional (all tests pass), create `chore(web): regenerar Orval client`
   2. Steps:
      - Stop server :3001 in main checkout
      - Switch to worktree
      - Start server :3001 in worktree
      - Run `pnpm --filter @app/web generate:api`
      - Commit auto-generated files (10 files: proposals.zod.ts + 9x Details models)
   3. Then proceed with code review of feature logic (refactor phase)
   4. Reason: Separates auto-gen changes from hand-written code in diff review
   ```

2. **Editar `bens-code-rules.md` — adicionar regra "Type guards obrigatórios para enums"**
   - **Reason:** 4+ PRs recentes usaram type guards; padrão está consolidado; novo skill deveria mencioná-lo.
   - **Suggested diff:**

   ````markdown
   #### Type guards for discriminated unions + enums

   When exporting enum or discriminated union type from shared package:

   1. Create type guard function: `is<Type>(value: unknown): value is Type`
   2. Use Zod `.safeParse()` if schema exists, or manual discriminant check
   3. Apply in consume code: prefer type guard over `as Type` assertions
   4. Example:
      ```ts
      // shared/insured-object-details-schema.ts
      export function isBusinessSegment(
        value: unknown
      ): value is BusinessSegment {
        return BUSINESS_SEGMENT_VALUES.includes(value as never)
      }
      ```
   ````

   5. Consumer code:
      ```ts
      const segment = isBusinessSegment(raw) ? raw : null
      ```

   Rationale: Eliminates `// @ts-expect-error` comments; type-safe fallbacks; aligns with ABSOLUTE PROHIBITIONS (NO `as` type assertions outside tests).

   ```

   ```

## Sinais de sucesso pós-aplicação

1. **Orval workflow decision memory** → Próximo PR com Details field novo planeja e comenta Orval regen step ANTES de implementar; não decide ad-hoc
2. **Type guards rule em bens-code-rules** → Code reviewer flaga `as Type` assertions menos frequentemente; quando flaga, já há memory entry pra tipo guard pattern
3. **bens-implementation-flow Orval seção** → User queries durante IMPLEMENT phase sobre "when to regen Orval?" acham resposta em skill, não escalada pra AskUserQuestion

---

## Contexto Adicional

**PR Timeline:**

- 2026-05-20 03:35 → 03:50: Phases 1-4 (spec+plan) — paused awaiting PR-3/4/5 merge
- 2026-05-20 21:01 → 23:39: Resume Phase 5-10 (implement+code-review+QA+CI)
- 2026-05-20 23:39: Merge #318

**Key decisions escalated to user:**

1. Ambiguidade spec: branchType (enum) vs businessActivity (freetext) → Resolvido: ambos coexistem, branchType → businessSegment (enum enum), activity é freetext
2. Orval regen timing: now vs post-refactor → User OK com now (cleaner diff)
3. Resume pós-pause: continuar de Phase 5 vs restart → User chose continue

**Files touched:**

- Feature: shared (enum + helper), web (labels + form logic + display)
- Orval auto-gen: 10 files (zod + model details)
- Tests: 2 specs (83 + 108 lines)
- Total: 18 files, +656 -413
